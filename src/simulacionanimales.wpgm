import wollok.game.*
import animales.*

program animales {
	// configuracion de pantalla
	game.width(15)
	game.height(15)

	/*
	 * agregamos personajes
	 */ 
	var animalActual = new Vaca()

	/*
	 * configuraciones de teclado
	 */
	
	/*
	 * arrancamos el juego
	 */
	 game.start()
}